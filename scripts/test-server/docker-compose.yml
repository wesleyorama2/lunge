version: '3.8'

services:
  test-server:
    build: .
    ports:
      - "80:80"
    restart: unless-stopped
    # Optimize for performance
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    # Use host network for better performance (optional)
    # network_mode: host
